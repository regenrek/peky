name: split-v
description: "Two vertical panes (left/right)"

settings:
  bind_keys:
    - key: g
      action: "switch-client -t peakypanes"

windows:
  - name: dev
    layout: even-horizontal
    panes:
      - title: left
        cmd: ""
      - title: right
        cmd: ""
