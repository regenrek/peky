name: dev-2
description: "Simple two-pane layout: editor left, terminal right"

windows:
  - name: dev
    panes:
      - title: editor
        cmd: "${EDITOR:-}"
        size: "60%"
      - title: shell
        cmd: ""
        split: horizontal
